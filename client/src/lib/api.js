const BASE=import.meta.env.VITE_API_URL||'http://localhost:5000/api/v1';let token=null;export const setAccessToken=t=>{token=t};
async function request(path,options={},retried=false){const headers={'Content-Type':'application/json',...(options.headers||{})};if(token)headers.Authorization=`Bearer ${token}`;let r=await fetch(`${BASE}${path}`,{...options,headers,credentials:'include'});if(r.status===401&&!retried&&!path.startsWith('/auth/')){const refresh=await fetch(`${BASE}/auth/refresh`,{method:'POST',credentials:'include'});if(refresh.ok){token=(await refresh.json()).data.accessToken;return request(path,options,true);}}const json=await r.json().catch(()=>({message:'Network error'}));if(!r.ok)throw new Error(json.message||'Request failed');return json;}
export const api={get:p=>request(p),post:(p,data)=>request(p,{method:'POST',body:JSON.stringify(data)}),patch:(p,data)=>request(p,{method:'PATCH',body:JSON.stringify(data)}),delete:p=>request(p,{method:'DELETE'})};

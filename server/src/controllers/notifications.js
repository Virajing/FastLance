import Notification from '../models/Notification.js';import {asyncHandler,ok} from '../utils/api.js'; export const list=asyncHandler(async(req,res)=>{const rows=await Notification.find({user:req.user._id}).sort('-createdAt').limit(50);ok(res,{notifications:rows,unreadCount:await Notification.countDocuments({user:req.user._id,read:false})});});export const read=asyncHandler(async(req,res)=>{await Notification.updateOne({_id:req.params.id,user:req.user._id},{read:true});ok(res,{},'Notification read');});export const readAll=asyncHandler(async(req,res)=>{await Notification.updateMany({user:req.user._id,read:false},{read:true});ok(res,{},'Notifications read');});
